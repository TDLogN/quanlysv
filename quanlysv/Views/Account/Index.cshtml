@model IEnumerable<QuanLySinhVien.Models.Account>

@{
    ViewData["Title"] = "Danh sách tài khoản";
}

<h1>Danh sách tài khoản 🔒</h1>
<partial name="../Shared/_search" />
<hr />

<div class="d-flex justify-content-between align-items-center mb-3">
    @* <a asp-action="Create" class="btn btn-primary">
        <i class="fas fa-plus"></i> Thêm sách tài khoản
    </a> *@
</div>

<div class="card shadow-sm">
    <div class="card-body p-0">
        <table class="table table-hover table-striped mb-0">
            <thead class="table-dark">
                @* <tr>
                    <th>@Html.DisplayNameFor(model => model.AccountID) </th> *@
                    <th>@Html.DisplayNameFor(model => model.Username) </th>
                    <th>@Html.DisplayNameFor(model => model.Role) </th>
                    <th style="width: 150px;">Hành động</th>
                </tr>
            </thead>
            <tbody>
                <partial name="_AccountTableBody" model="Model" />
            </tbody>
        </table>
    </div>
</div>
<partial name="../Shared/_PageList" />